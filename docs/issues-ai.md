| Severity | Location (File:Line) | Core Problem | Recommended Fix / Refactor |
| --- | --- | --- | --- |
| **🟠 High (1)** | `backend/ops/stats.py:46` | **[BUG-8]** `r2 = float("nan")` is serialized by Starlette's `json.dumps(allow_nan=True)` as the literal token `NaN`, which is invalid JSON; the browser `response.json()` throws and the whole regression result fails to load. | Return `null` (Python `None`) for undefined R², or a `{r2: null, r2_defined: false}` shape; never emit NaN/Inf across the JSON boundary. |
| **🟡 Med (1)** | `frontend/src/ui/GraphCanvas.tsx:91-126` | **[BUG-5]** Shading rects drawn in device-pixel coords; if `drawAxes` uses CSS-pixel space / DPR transform, grid and axes misalign on `devicePixelRatio ≠ 1`. | Runtime DPR check to confirm/fix 2D context scaling consistency. |
| **🟡 Med (2)** | `frontend/src/plugins/sandbox.worker.ts:78` | **[SEC-7]** Egress control relies on the document's *meta-tag* CSP `connect-src` governing the worker; module workers often take CSP from their own response headers, not the parent meta tag, so `fetch`/`WebSocket` exfil may not actually be blocked. | Serve a CSP response header for the worker script (or verify enforcement per-engine); don't rely on meta CSP for worker egress. |
| **🟡 Med (3)** | `frontend/src/plugins/sandbox.ts:196` | **[ARCH-6]** A fresh `Worker` is constructed and `terminate()`d on every single `runSandboxed` call; module-worker spin-up + bundle load per invocation is costly for tools run interactively or in tight loops. | Pool/reuse one long-lived worker keyed by lifecycle; only recreate after a timeout-kill. |
| **🟡 Med (4)** | `frontend/src/store.ts:214` | **[BUG-11]** `color: e.color ?? colorFor(0)` assigns palette index 0 to every uncolored loaded expression, collapsing all curves to one color (latent today only because `deserialise` always sets a color). | Index the fallback: `e.color ?? colorFor(i)` using the map index. |
| **🟡 Med (5)** | `backend/main.py:43` | **[ARCH-7]** `@app.on_event("startup")` is deprecated and slated for removal in current FastAPI/Starlette. | Migrate to the `lifespan=` async context manager on `FastAPI(...)`. |
| **🟡 Med (6)** | Frontend/Backend Configuration | **[ENV-3]** Frontend `package.json` `1.0.0` vs backend FastAPI `2.0.0` vs registry `version: 1`; no `app/package.json` (Electron app has no version/`appId`/packaging+signing config); no git-SHA/build-date injection; README/CLAUDE.md reference root `install.bat` but scripts live in `sysops/`. | Single source of truth (`VERSION`/root `package.json`) injected into both ends; add `app/package.json`. |
| **🟡 Med (7)** | `frontend/Dockerfile`, `docker-compose.yml` | **[ENV-4]** CMD is `npm run dev`; no `vite build` + nginx static path. Conflicts with `read_only: true` (BP-3). | Multi-stage build (`vite build` $\rightarrow$ nginx) for prod compose; dev server only in `docker-compose.dev.yml`. |
| **🟡 Med (8)** | `frontend/package.json` | **[ENV-5]** Exact pins mixed with carets on security-relevant deps (`vite`, `electron`, `vitest`, `react-markdown`); `install.sh`/`.bat` use `npm install` not `npm ci`. | `npm ci` in install scripts; consistent lockfile reliance. |
| **🟡 Med (9)** | Continuous Integration | **[BP-1]** No `.github/workflows`, no pre-commit. | Pipeline running `npm ci && npm run lint && npm test` + `pytest`. |
| **🟢 Low (1)** | `frontend/src/renderer/hittest.ts:24-36`, `GraphCanvas.tsx` | **[BUG-10]** Distance is `hypot(Δx, Δy)` in math units while tolerance is derived from x-range only. Correct only while `fitAspect` keeps units square; anisotropic otherwise. | Not yet addressed (conditional / low risk). |
| **🟢 Low (2)** | `frontend/src/project.ts`, `store.ts` | **[ENV-6]** `deserialise` emits attacker-controlled ids; live `loadProject` reassigns via `newId()` (safe path) but direct consumers / `nextId` collisions risk key clashes. | Validate/normalize ids on load. |
| **🟢 Low (3)** | `.claude/settings.local.json`, `CLAUDE.md` | **[ENV-7]** Absolute user paths, permission allow-list, account ID (ARCH-5). `.gitignore` excludes `.claude/`+`CLAUDE.md` so they should be untracked. | Confirm untracked status with `git ls-files`. |
| **🟢 Low (4)** | `backend/main.py` | **[ENV-8]** `/docs`, `/redoc`, `/openapi.json` exposed. | Fine locally; set `docs_url=None` for any shared deployment. |
| **🟢 Low (5)** | `app/main.ts:38` | **[ENV-9]** Prod nav guard allows any `file://` URL, so a compromised renderer can navigate to arbitrary local files (`file:///...`). | Restrict to the app index path prefix (`PROD_INDEX` resolved URL), not the bare `file://` scheme. |
| **🟢 Low (6)** | `frontend/src/ui/toolpanels/GenericPanel.tsx:174` (also `AnalyzePanel.tsx:45`) | **[BUG-12]** `autoRanRef` is never reset when `def.id`/`entry.id` change; a reused panel instance won't auto-run for a new expression. | Reset `autoRanRef.current = false` at the top of the effect before the guard, or key the component on `entry.id`. |
| **🟢 Low (7)** | `backend/ops/parse_utils.py:18` | **[BUG-13]** `_POWER_TOWER` regex rejects legitimate finite height-3 numeric towers (e.g. `2^3^2`=512), not just astronomical ones; over-broad denial. | Gate on the evaluated exponent magnitude, or only block when both upper exponents exceed a small threshold. |
| **🟢 Low (8)** | `backend/ops/parse_utils.py:24` | **[BUG-14]** `_GLOBAL_DICT["__builtins__"] = {}` can break `parse_expr` for inputs that legitimately resolve through builtins during sympify. | Whitelist a minimal safe builtins subset instead of emptying it; test common inputs. |
| **🟢 Low (9)** | `frontend/src/renderer/overlays.ts:33` | **[BUG-15]** `parseColor` does not clamp channels; `rgb(300,0,0)` yields >1 and feeds an out-of-range value to the WebGL color uniform. | Clamp r/g/b/alpha to `[0,1]` after parse. |
| **🟢 Low (10)** | `frontend/eslint.config.js` | **[BP-2]** `no-unused-vars`/`no-unused-expressions` off, `no-explicit-any` only `warn`; backend has no `ruff`/`flake8`. | Re-enable safe rules; add Python linter. |
| **🟢 Low (11)** | `docker-compose.yml`, `frontend/Dockerfile` | **[BP-3]** Read-only production deployment container conflicts with active dev server workspace caching operations. | Resolved once ENV-4 lands (static build). |
| **🟢 Low (12)** | `sample_plugins/*.ngplugin.json` | **[BP-4]** Single escaped `js` line = unreviewable. | Separate readable files + integrity hashes. |
| **🟢 Low (13)** | `frontend/vite.config.mjs` | **[BP-5]** `environment: "node"` $\rightarrow$ React components untestable. | Confirm intent or add jsdom project. |
| **🟢 Low (14)** | `rsync.sh` | **[BP-6]** Personal backup script; probably shouldn't be in the shared repo. | Purge external machine helper binaries from active code scopes. |
| **⚪ Note (1)** | `frontend/src/engine/sampler.ts`, `GraphCanvas.tsx` | **[ARCH-3]** Every viewport change re-runs full-grid evaluation per implicit/inequality expression, no caching/debounce. | Performance only; implement sampling debouncers or cache grid calculations. |
| **⚪ Note (2)** | `backend/compute.py` | **[ARCH-4]** All ops run synchronously in the request thread. SEC-3 caps reduce worst case but one heavy op still blocks. | Establish a worker pool paired with structural execution timeouts. |
| **⚪ Note (3)** | `CLAUDE.md` | **[ARCH-5]** Checked-in account ID, hostname, "bypass tracking permission," mandatory external-routing directives — posture / secrets-hygiene + injection surface. | Flagged for internal review; remove hardware tracking artifacts. |
| **⚪ Note (4)** | `frontend/src/engine/evaluator.ts:13` | **[ARCH-8]** Recursion backstop is a module-level mutable `callDepth` and silently returns `NaN` at the cap; depth is shared across all evaluate calls and the cap (256) could clip deep-but-valid composition. | Acceptable, but document the cap as user-visible behavior and pair with the store cycle error so silent NaN isn't the only signal. |
| **⚪ Note (5)** | `frontend/index.html:14` | **[ENV-10]** CSP still grants `script-src 'unsafe-eval'` to the whole document even though plugin eval moved into the worker realm; main realm retains eval capability. | Scope `unsafe-eval` to the worker only (separate worker CSP) and drop it from the document if nothing else needs it. |
| **⚪ Note (6)** | `frontend/src/engine/parser.ts:74` | **[ENV-11]** New trailing-token check turns previously silently-accepted inputs (trailing junk) into parse errors; may surface as errors on older saved `.ngraph` expressions. | Intended hardening — note in changelog; no code change. |
| **⚪ Note (7)** | `frontend/src/store.ts:281` | **[BP-7]** Assignment resolution re-evaluates each unresolved assignment every pass ($O(\text{passes} \times \text{assignments})$); fine at current scale but $O(n^2)$ on heavily interdependent docs. | Optional: topologically order assignments via the existing dependency graph and resolve in one pass. |